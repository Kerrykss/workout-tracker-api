// src/controladores/tareas.controlador.js

// Controlador temporal para probar la ruta de tareas
export const obtenerTareas = (req, res) => {
  res.json({
    mensaje: "Aquí se listarán las tareas más adelante 📝",
  });
};
